boilerplate:
  outputs:
    dev:
      type: postgres
      host: "localhost"
      port: 5432
      user: "sample_user"
      pass: "sample_password"
      dbname: "playground"
      schema: "default"
      threads: 4
  target: dev
